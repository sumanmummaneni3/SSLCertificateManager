CertGuard Cloud — Certificate Expiry WARNING

Target:    [(${host + ':' + port})]
Days Left: [(${daysLeft})]
Severity:  [(${severity})]

The SSL/TLS certificate will expire in [(${daysLeft})] day(s).

[# th:if="${agentDiscovered}"]
View your certificate and request renewal here:
[(${deepLinkUrl})]
[/]
[# th:unless="${agentDiscovered}"]
For automatic renewal please use the agent and configure it.

Log in to CertGuard Cloud to view details: [(${baseUrl})]
[/]

— CertGuard Cloud
