spring:
  application:
    name: certguard-auth-service
  threads:
    virtual:
      enabled: true

  datasource:
    url: ${AUTH_DB_URL:jdbc:postgresql://localhost:5432/certguard_auth}
    username: ${AUTH_DB_USERNAME:auth_user}
    password: ${AUTH_DB_PASSWORD:auth_pass}
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 3
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default_schema: public

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: false

server:
  port: ${AUTH_SERVER_PORT:8090}
  error:
    include-message: always
    include-binding-errors: always
    include-stacktrace: never
    include-exception: false

auth:
  dev-mode: ${AUTH_DEV_MODE:false}

  platform-admin:
    emails: ${PLATFORM_ADMIN_EMAILS:}

  jwt:
    private-key-path: ${AUTH_JWT_PRIVATE_KEY_PATH:/opt/certguard-auth/certs/jwt-private.pem}
    public-key-path: ${AUTH_JWT_PUBLIC_KEY_PATH:/opt/certguard-auth/certs/jwt-public.pem}
    expiration-ms: ${AUTH_JWT_EXPIRATION_MS:3600000}
    issuer: certguard-cloud
    audience:
      - certguard-ui
      - certguard-apps

  google:
    client-id: ${GOOGLE_CLIENT_ID:}
    client-secret: ${GOOGLE_CLIENT_SECRET:}

  microsoft:
    client-id: ${MICROSOFT_CLIENT_ID:}
    client-secret: ${MICROSOFT_CLIENT_SECRET:}
    tenant-id: ${MICROSOFT_TENANT_ID:common}

  callback:
    base-url: ${AUTH_CALLBACK_BASE_URL:http://localhost:8090}

  cors:
    allowed-origins: ${AUTH_CORS_ALLOWED_ORIGINS:http://localhost:3000,http://localhost:5173}

  rate-limit:
    max-attempts: ${AUTH_RATE_LIMIT_MAX_ATTEMPTS:10}
    window-seconds: ${AUTH_RATE_LIMIT_WINDOW_SECONDS:300}
    cleanup-interval-ms: ${AUTH_RATE_LIMIT_CLEANUP_MS:3600000}

  session:
    cleanup-interval-ms: ${AUTH_SESSION_CLEANUP_MS:3600000}

main:
  datasource:
    url:      ${MAIN_DB_URL:jdbc:postgresql://localhost:5432/certguard}
    username: ${MAIN_DB_USERNAME:certguard_user}
    password: ${MAIN_DB_PASSWORD:certguard_pass}
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
      connection-timeout: 30000

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when-authorized

logging:
  level:
    com.certguard.auth: INFO
    org.springframework.security: WARN
    org.flywaydb: INFO
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
